version: "3.9"

services:
  java-web-app:
    image: 66raven99/java-web-app:latest
    container_name: java-web-app
    ports:
      - "8080"   # Only specify container port; Docker will pick a free host port automatically
    environment:
      - PORT=8080
    restart: unless-stopped
